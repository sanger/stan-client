query FindLabware($barcode: String!) {
    labware(barcode: $barcode) {
        barcode
        labwareType {
            name
        }
        slots {
            address
            block
            samples {
                id
                tissue {
                    donor {
                        donorName
                    }
                    spatialLocation {
                        tissueType {
                            name
                        }
                        code
                    }
                    replicate
                }
            }
        }
    }
}