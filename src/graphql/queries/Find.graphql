query Find($request: FindRequest!) {
    find(request: $request) {
        numRecords
        entries {
            labwareId
            sampleId
        }
        samples {
            id
            section
            tissue {
                replicate
                spatialLocation {
                    tissueType {
                        name
                    }
                }
                externalName
                donor {
                    donorName
                }
            }
        }
        labware {
            id
            barcode
        }
        locations {
            id
            barcode
            customName
            name
        }
        labwareLocations {
            labwareId
            locationId
            address
        }
    }
}