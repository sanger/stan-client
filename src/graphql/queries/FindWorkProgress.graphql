query FindWorkProgress($workNumber: String, $workTypes: [String!], $programs: [String!], $statuses: [WorkStatus!],$recipients:[String!]!) {
  workProgress(workNumber: $workNumber, workTypes: $workTypes, programs: $programs, statuses: $statuses,recipients:$recipients) {
    ...WorkProgressFields
  }
}
