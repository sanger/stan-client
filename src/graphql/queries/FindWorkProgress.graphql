query FindWorkProgress($workNumber: String, $workTypes: [String!], $programs: [String!], $statuses: [WorkStatus!]) {
  workProgress(workNumber: $workNumber, workTypes: $workTypes, programs: $programs, statuses: $statuses) {
    ...WorkProgressFields
  }
}
