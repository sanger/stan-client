query FindWorkProgress($workNumber: String, $workTypes: [String!], $statuses: [WorkStatus!]) {
  workProgress(workNumber: $workNumber, workTypes: $workTypes, statuses: $statuses) {
    ...WorkProgressFields
  }
}
