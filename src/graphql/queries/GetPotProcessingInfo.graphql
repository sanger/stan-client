query GetPotProcessingInfo {

    fixatives {
        name
    }
    comments(includeDisabled: false, category: "Tissue Pot processing") {
        ...CommentFields
    }
    labwareTypes {
        ...LabwareTypeFields
    }
}