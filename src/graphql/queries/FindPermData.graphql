query FindPermData($barcode: String!) {
    visiumPermData(barcode: $barcode) {
        labware {
            ...LabwareFields
        }
        addressPermData {
            address
            controlType
            seconds
            selected
        }
    }
}