query FindHistory($workNumber: String, $barcode: String, $donorName: String, $externalName: String) {
  history(workNumber: $workNumber, barcode: $barcode, donorName: $donorName, externalName: $externalName) {
    ...HistoryFields
  }
}
