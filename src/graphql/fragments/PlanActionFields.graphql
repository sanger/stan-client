fragment PlanActionFields on PlanAction {
  newSection
  sampleThickness
  sectioningOrder
  sample {
    id
  }
  source {
    address
    labwareId
    samples {
      id
      tissue {
        ...TissueFields
      }
    }
  }
  destination {
    address
    labwareId
  }
}
