fragment SampleFields on Sample {
    id
    section
    tissue {
        donor {
            donorName
        }
        externalName
        spatialLocation {
            tissueType {
                name
            }
            code
        }
        replicate
    }
    bioState {
        name
    }
}
