fragment SampleFields on Sample {
    id
    tissue {
        donor {
            donorName
        }
        externalName
        spatialLocation {
            tissueType {
                name
            }
            code
        }
        replicate
    }
}
