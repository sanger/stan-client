mutation Segmentation($request: SegmentationRequest!) {
    segmentation(request: $request) {
        operations {
            id
        }
        labware {
            ...LabwareFields
        }
    }
}
