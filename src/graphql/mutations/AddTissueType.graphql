mutation AddTissueType($request: AddTissueTypeRequest!) {
    addTissueType(request: $request) {
        name
        code
        spatialLocations {
            code
            name
        }
    }
}