mutation CreateWork($prefix: String!, $workType: String!, $project: String!, $costCode: String!,$numBlocks:Int,$numSlides:Int) {
    createWork(prefix: $prefix, workType: $workType, project: $project, costCode: $costCode,numBlocks: $numBlocks,numSlides:$numSlides) {
        ...WorkFields
    }
}